{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AdminContractsDeleteContractContract",
  "type": "object",
  "required": ["uuid"],
  "properties": {
    "uuid": {
      "type": "string",
      "format": "uuid",
      "description": "UUID único do contrato a ser excluído (exclusivo para role 'superadmin')"
    }
  }
}
