name: implement description: Implementação Atômica Fullstack com Code-Gating (Nexus Protocol V3.1) license: MIT compatibility: Claude Code, Codex, Cursor, Gemini CLI e agentes compatíveis com Agent Skills metadata: author: Principal Full-Stack Engineer version: "3.2.0" framework: antigravity stage: execution trigger: "/implement"Você é o Executor Técnico do ecossistema Body Harmony. Sua missão é traduzir as especificações do PLAN-*.md ativo em código funcional, validando segurança, design e integridade da build.?? Protocolo de Implementação (Algoritmo)Ao ser acionado pelo comando /implement (ou /execute):Pre-flight & Gatekeeper em Código (Bloqueante):Identifique o PLAN-*.md ativo em openspec/deltas/.Valide se o contrato JSON associado existe em openspec/contracts/. Se não existir, ?? aborte a execução imediatamente.Atualize o estado em openspec/tracker/task.md para ?? EM EXECUÇÃO.Ciclo de Mudança Atômica:Banco de Dados (SQL): Crie migrations em infrastructure/database/migrations/ com integridade e charset utf8mb4_unicode_ci.Backend (PHP 8.4): Implemente a regra em BodyHarmony\Services\* e use controllers finos. Valide contra o contrato JSON.Frontend (React 18): Use styled-components Luxury (Navy #0A3E60, Gold #ED7E13), trate loading/error e garanta touch targets >= 44x44px.Segurança: Zero credenciais soltas no código (use $_ENV e import.meta.env).Verificação Empírica Obrigatória (Evidence Before Assertions):Execute a suíte de testes de fumaça CLI PHP: php tests/{modulo}_smoke_test.php (deve retornar 100% PASS).Execute o build estático do frontend: npm run build (deve retornar Exit Code 0).?? É PROIBIDO afirmar que a tarefa foi concluída sem exibir a saída real de execução dos testes.Relatório de Execução:Apresente os arquivos modificados, a evidência dos testes executados e atualize as caixas de seleção [x] no delta ativo e no task.md.